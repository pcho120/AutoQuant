=== Task 13 Hardening Complete ===

## Changes Made

### 1. Demo/Missing Key Short-Circuit (Line 36-37)
Added explicit check before making external requests:
- Returns [] immediately if api_key is empty, None, or matches demo patterns
- Demo patterns: "DEMO_KEY", "test-key", "test"
- Prevents wasteful HTTP calls with invalid credentials

### 2. API Response Validation (Line 60)
Strengthened JSON response validation with dual checks:
- Validates: data.get("status") == "ok" (NewsAPI success indicator)
- Validates: isinstance(data.get("articles"), list) (type safety)
- Only returns articles if BOTH conditions met
- Prevents malformed/error responses from propagating downstream

## Verification Results

✓ py_compile: No syntax errors
✓ Demo keys: DEMO_KEY, test-key, test, empty string → all return []
✓ API validation: status='ok' check present
✓ Type validation: isinstance(articles, list) check present
✓ Sentiment: Unchanged, bounded to [-1.0, 1.0]
✓ LSP diagnostics: Zero errors/warnings

## Test Coverage

1. DEMO_KEY → [] (no request)
2. test-key → [] (no request)
3. test → [] (no request)
4. Empty string → [] (no request)
5. Positive sentiment → +1.0
6. Negative sentiment → -1.0
7. Neutral sentiment → 0.0

## Impact

- More robust for Tasks 14-15 (PredictionService integration)
- No performance penalty for demo environments (skips HTTP entirely)
- Better error handling for malformed API responses
- Maintains backward compatibility (same interface)

## Files Modified
- adapters/news_provider.py (lines 36-37, 60 added)

Status: ✅ HARDENING COMPLETE
